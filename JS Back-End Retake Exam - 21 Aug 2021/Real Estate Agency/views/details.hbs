            <section id="deatils-page">
                <div class="wrapper">
                    <div class="product-img">
                        <img src="/static/images/apartments.jpg">
                    </div>
                    <div class="product-info">
                        <div class="product-text">
                            <div class="product-text">
                                <h1>Name: {{rent.name}}</h1>
                                <h2>Type: {{rent.type}}</h2>
                                <h4>Year: {{rent.year}}</h4>
                                <h4>City: {{rent.city}}</h4>
                                <p>Description: {{rent.description}}</p>

                                <!-- if there are already tenants of the housing, separate their names with a comma and a space ", "  -->
                                {{#if rent.allRented}}
                               
                                <p>People rented this housing: {{rent.allRented}}</p>
                      
                                {{else}}
                                <!-- If not display: -->
                                <p>People rented this housing: There are no tenants yet.</p>
                                {{/if}}
                            </div>
                        </div>
                         {{#if username}}
                        <!-- if there is no registered user, do not display buttons-->
                        <div class="product-btn">
                            {{#if rent.isOwner}}
                            <!-- Only for registered user and creator of the housing offer-->
                            <a href="/rent/{{rent._id}}/edit" class="edit">Edit</a>
                            <a href="/rent/{{rent._id}}/delete" class="remove">Delete</a>
                            {{else}}
                            <!-- logged in user with available pieces-->
                            {{#if rent.isFull}}
                            <!-- logged in user with no available pieces-->
                            <p class="no-housing">No Housing Available!</p>
                            {{else}}
                            {{#if rent.isRent}}
                            <p class="alRentHome">You have already rent this home</p>
                            {{else}}
                            <a href="/rent/{{rent._id}}/takerent" class="rentHome">Rent a home, available {{rent.pieces}} housing</a>
                            {{/if}}

                            {{/if}}
                            {{/if}}
                        </div>
                        {{/if}}
                    </div>
                </div>
            </section>
